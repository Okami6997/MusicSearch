services:
  songsfetch:
    build: .
    container_name: songsfetch
    ports:
      - "3000:3000"
    volumes:
      - ./music:/music
    environment:
      - SONGSFETCH_OUTPUT_DIR=/music
    restart: unless-stopped
